export const selectAlignment = (alignment) => ({
  type: "SELECT_ALIGNMENT",
  alignment
});
selectAlignment.type = "SELECT_ALIGNMENT";

export const setModalVisibility = (visible) => ({
  type: "SET_ALIGNMENT_MODAL_VISIBILITY",
  visible
});
setModalVisibility.type = "SET_ALIGNMENT_MODAL_VISIBILITY";
